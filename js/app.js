// import our web components
import InterviewQuestionPanel from "./components/InterviewQuestionPanel.js";
import RecordingControls from "./components/RecordingControls.js";

// Initialize the app when the DOM is loaded
document.addEventListener("DOMContentLoaded", () => {
  // The web components will initialize themselves when connected to the DOM
  console.log("Interview Practice App initialized");
});
